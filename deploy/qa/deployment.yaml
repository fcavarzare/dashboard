apiVersion: apps/v1
kind: Deployment
metadata:
  name: dashboard
  namespace: dashboard-qa
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dashboard
  template:
    metadata:
      labels:
        app: dashboard
    spec:
      containers:
        - name: dashboard
          image: fcavarzare/dashboard-k8s:2
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
