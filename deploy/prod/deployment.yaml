apiVersion: apps/v1
kind: Deployment
metadata:
  name: dashboard
  namespace: dashboard-prod
spec:
  replicas: 6
  selector:
    matchLabels:
      app: dashboard
  template:
    metadata:
      labels:
        app: dashboard
    spec:
      containers:
        - name: dashboard
          image: fcavarzare/dashboard-k8s:zz
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
